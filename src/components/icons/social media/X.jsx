import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

import { faXTwitter } from "@fortawesome/free-brands-svg-icons";

const X = ({ styles }) => {
  //   return <FontAwesomeIcon icon={faTwitter} className={styles.icon} />;
  return <FontAwesomeIcon icon={faXTwitter} className={styles.icon} />;
};

export default X;
